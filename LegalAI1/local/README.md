# Local Helper Scripts

These are one-off local helper scripts that should not live in the repo root.

They are kept here until each one is either:

- promoted into a real maintained script
- rewritten into a documented workflow
- or deleted as no longer needed
